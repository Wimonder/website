{
	"name": "monorepo",
	"private": true,
	"workspaces": [
		"site",
		"cms"
	],
	"scripts": {
		"build": "turbo run build",
		"lint": "turbo run lint",
		"format": "turbo run format",
		"dev": "turbo run dev",
		"web:build": "turbo run build --filter=web"
	},
	"devDependencies": {
		"eslint": "^8.22.0",
		"prettier": "^2.6.2",
		"turbo": "^1.4.5",
		"typescript": "^4.9.3"
	}
}
